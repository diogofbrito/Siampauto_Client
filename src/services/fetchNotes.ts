/* import { api } from './api';

interface Note {
	id: number;
	title: string;
	date: Date;
	content: string;
	createdAt: Date;
	updatedAt: Date;
}

export async function fetchNotes() {
	const response = await api.get<Note[]>('/notes');
	re turn response.data;
}
*/